instalações de dependencias
Express - Node.js web application framework

→ npm install express --save
→ npm install nodemon --save-dev
→ npm install dotenv
→ npm install mongoose --save
→ npm install mustache-express --save

req - informações da requisição, cabeçalhos, parametros enviados, arquivos
    GET - 
        http://localhost:7777/?nome=Felipe&idade=25
        req.query.nome 
        req.query.idade
    POST - 
        router.get('/posts/:id', (req,res) 
        req.params.id
        req.body 



res -   res.json(req.query)
        res.send('enviando alguma resposta')

